{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run Script: start:debug",
      "request": "launch",
      "command": "NODE_ENV=development pnpm run start:debug",
      "cwd": "${workspaceFolder}"
    }
  ]
}